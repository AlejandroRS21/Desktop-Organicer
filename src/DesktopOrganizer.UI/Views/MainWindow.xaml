<Window x:Class="DesktopOrganizer.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopOrganizer.UI.Views"
        mc:Ignorable="d"
        Title="Desktop Organizer" Height="600" Width="900"
        Background="#F5F7FA"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2868A6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E8EDF3"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D1DAE6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card Style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E2E8F0" Direction="270" ShadowDepth="2" BlurRadius="12" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" Padding="30,20">
            <Border.Effect>
                <DropShadowEffect Color="#E2E8F0" Direction="270" ShadowDepth="1" BlurRadius="8" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Logo/Icon -->
                <Ellipse Width="50" Height="50" Fill="#4A90E2" Margin="0,0,15,0">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="#4A90E2" BlurRadius="10" Opacity="0.3"/>
                    </Ellipse.Effect>
                </Ellipse>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Desktop Organizer" FontSize="28" FontWeight="Bold" Foreground="#2D3748"/>
                    <TextBlock Text="Mantén tu escritorio ordenado automáticamente" FontSize="13" Foreground="#718096" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Status Card -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Status Indicator -->
                    <Ellipse Width="12" Height="12" Margin="0,0,12,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#48BB78"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMonitoring}" Value="False">
                                        <Setter Property="Fill" Value="#CBD5E0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding StatusMessage}" FontSize="15" FontWeight="SemiBold" Foreground="#2D3748"/>
                        <TextBlock Text="El monitoreo está activo y organizando archivos automáticamente" 
                                   FontSize="12" Foreground="#718096" Margin="0,4,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMonitoring}" Value="False">
                                            <Setter Property="Text" Value="Haz clic en 'Iniciar Monitoreo' para comenzar"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <!-- Main Action Button -->
                    <Button Grid.Column="2" 
                            Content="{Binding StartStopButtonText}"
                            Command="{Binding ToggleMonitoringCommand}"
                            Style="{StaticResource ModernButton}"
                            MinWidth="160"/>
                </Grid>
            </Border>

            <!-- Action Cards -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Manage Rules Card -->
                <Border Style="{StaticResource Card}" Margin="0,0,10,0">
                    <StackPanel>
                        <Ellipse Width="48" Height="48" Fill="#E8F4FD" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                        <TextBlock Text="Gestionar Reglas" FontSize="16" FontWeight="SemiBold" Foreground="#2D3748" Margin="0,0,0,6"/>
                        <TextBlock Text="Configura cómo organizar tus archivos" FontSize="12" Foreground="#718096" TextWrapping="Wrap" Margin="0,0,0,16"/>
                        <Button Content="Abrir Editor" 
                                Command="{Binding OpenRuleEditorCommand}"
                                Style="{StaticResource SecondaryButton}"
                                HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- View Logs Card -->
                <Border Grid.Column="1" Style="{StaticResource Card}" Margin="5,0,5,0">
                    <StackPanel>
                        <Ellipse Width="48" Height="48" Fill="#FEF5E7" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                        <TextBlock Text="Ver Historial" FontSize="16" FontWeight="SemiBold" Foreground="#2D3748" Margin="0,0,0,6"/>
                        <TextBlock Text="Revisa las operaciones realizadas" FontSize="12" Foreground="#718096" TextWrapping="Wrap" Margin="0,0,0,16"/>
                        <Button Content="Ver Logs" 
                                Command="{Binding OpenLogsCommand}"
                                Style="{StaticResource SecondaryButton}"
                                HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Settings Card -->
                <Border Grid.Column="2" Style="{StaticResource Card}" Margin="10,0,0,0">
                    <StackPanel>
                        <Ellipse Width="48" Height="48" Fill="#F0FDF4" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                        <TextBlock Text="Configuración" FontSize="16" FontWeight="SemiBold" Foreground="#2D3748" Margin="0,0,0,6"/>
                        <TextBlock Text="Ajusta las carpetas monitoreadas" FontSize="12" Foreground="#718096" TextWrapping="Wrap" Margin="0,0,0,16"/>
                        <Button Content="Configurar" 
                                Command="{Binding OpenSettingsCommand}"
                                Style="{StaticResource SecondaryButton}"
                                HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Fences Management Panel -->
            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,20,0,0" Background="#F7FAFC">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="📁 Fences Activos" FontSize="16" FontWeight="SemiBold" Foreground="#2D3748"/>
                        <TextBlock Text="(Edita las extensiones para cambiar las reglas)" FontSize="12" Foreground="#718096" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Fences}" 
                             Background="Transparent" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             Height="180">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0,4"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Text="{Binding Name}" FontWeight="Medium" VerticalAlignment="Center" Foreground="#4A5568"/>
                                    
                                    <TextBox Grid.Column="1" Text="{Binding Extensions, UpdateSourceTrigger=PropertyChanged}" 
                                             Margin="10,0" Padding="8,4" VerticalContentAlignment="Center"
                                             Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                    
                                    <Button Grid.Column="2" Content="Guardar" 
                                            Command="{Binding DataContext.SaveRulesCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                            CommandParameter="{Binding}"
                                            Background="#E8EDF3" Foreground="#4A5568"
                                            BorderThickness="0" Padding="10,4"
                                            Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
